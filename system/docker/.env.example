# DevStack Environment Configuration

# Nginx Configuration
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# PHP Configuration
PHP_VERSION=8.2
PHP_MEMORY_LIMIT=256M
PHP_MAX_EXECUTION_TIME=30

# MySQL Configuration
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=devstack
MYSQL_USER=devstack
MYSQL_PASSWORD=changeme

# Redis Configuration (Optional)
REDIS_PASSWORD=changeme

# MailHog Configuration (Optional)
MAILHOG_UI_PORT=8025

# Project Settings
PROJECT_NAME=devstack
COMPOSE_PROJECT_NAME=devstack

# Additional Service Profiles
# Uncomment to enable optional services:
#COMPOSE_PROFILES=redis,mail

